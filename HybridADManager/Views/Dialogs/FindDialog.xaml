<Window x:Class="HybridADManager.Views.Dialogs.FindDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:HybridADManager.ViewModels"
        xmlns:models="clr-namespace:HybridADManager.Models"
        Title="Find"
        Width="700"
        Height="500"
        MinWidth="500"
        MinHeight="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Style="{StaticResource ADUCWindowStyle}"
        Background="{StaticResource WindowBackgroundBrush}">

    <Window.DataContext>
        <vm:FindDialogViewModel/>
    </Window.DataContext>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search Criteria -->
        <Border Grid.Row="0"
                Background="{StaticResource PaneBackgroundBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                Padding="8"
                Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="0,0,8,4">
                    <Label Content="Name:" Width="80" VerticalContentAlignment="Center"/>
                    <TextBox Text="{Binding SearchName, UpdateSourceTrigger=PropertyChanged}"
                             Width="200"
                             VerticalContentAlignment="Center"
                             Padding="4,2"
                             AutomationProperties.Name="Search Name"
                             ToolTip="Search by name"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,4">
                    <Label Content="Email:" Width="80" VerticalContentAlignment="Center"/>
                    <TextBox Text="{Binding SearchEmail, UpdateSourceTrigger=PropertyChanged}"
                             Width="200"
                             VerticalContentAlignment="Center"
                             Padding="4,2"
                             AutomationProperties.Name="Search Email"
                             ToolTip="Search by email"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Margin="0,0,8,4">
                    <Label Content="Description:" Width="80" VerticalContentAlignment="Center"/>
                    <TextBox Text="{Binding SearchDescription, UpdateSourceTrigger=PropertyChanged}"
                             Width="200"
                             VerticalContentAlignment="Center"
                             Padding="4,2"
                             AutomationProperties.Name="Search Description"
                             ToolTip="Search by description"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,4">
                    <Label Content="Phone:" Width="80" VerticalContentAlignment="Center"/>
                    <TextBox Text="{Binding SearchPhone, UpdateSourceTrigger=PropertyChanged}"
                             Width="200"
                             VerticalContentAlignment="Center"
                             Padding="4,2"
                             AutomationProperties.Name="Search Phone"
                             ToolTip="Search by phone"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,4,0,0">
                    <Button Content="Find Now"
                            Width="75" Height="23"
                            Margin="0,0,6,0"
                            Command="{Binding SearchCommand}"
                            AutomationProperties.Name="Find Now"
                            ToolTip="Start search"/>
                    <Button Content="Clear"
                            Width="75" Height="23"
                            Command="{Binding ClearCommand}"
                            AutomationProperties.Name="Clear Search"
                            ToolTip="Clear search criteria"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Results -->
        <Border Grid.Row="1"
                Background="{StaticResource PaneBackgroundBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                Margin="0,0,0,8">
            <ListView ItemsSource="{Binding Results}"
                      SelectedItem="{Binding SelectedResult}"
                      AlternationCount="2"
                      MouseDoubleClick="ResultsListView_MouseDoubleClick"
                      AutomationProperties.Name="Search Results"
                      ToolTip="Search results">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" Width="200">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate DataType="{x:Type models:DirectoryObject}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding ObjectType, StringFormat='[{0}] '}"
                                                   Foreground="{StaticResource ForegroundMutedBrush}"
                                                   FontSize="10"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,4,0"/>
                                        <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Type" Width="80"
                                        DisplayMemberBinding="{Binding ObjectType}"/>
                        <GridViewColumn Header="Email / UPN" Width="*"
                                        DisplayMemberBinding="{Binding Email}"/>
                        <GridViewColumn Header="Description" Width="200"
                                        DisplayMemberBinding="{Binding Description}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <!-- Error Message -->
        <TextBlock Grid.Row="2"
                   Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   TextWrapping="Wrap"
                   Margin="0,0,0,4"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyToVisibilityConverter}}"/>

        <!-- Status Bar and Close Button -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StatusBar Grid.Column="0">
                <StatusBarItem Content="{Binding StatusText}"/>
            </StatusBar>

            <Button Grid.Column="1"
                    Content="Close"
                    Width="75" Height="23"
                    Margin="8,0,0,0"
                    IsCancel="True"
                    AutomationProperties.Name="Close"
                    ToolTip="Close dialog"/>
        </Grid>
    </Grid>
</Window>
